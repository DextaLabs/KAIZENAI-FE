import { HTMLProps } from "react";

type className = HTMLProps<HTMLElement>["className"];

export const labelValue: className = "flex flex-col gap-1";

export const layoutSpacing: className = "p-[36px]";
